<%= form_with(model: locate, local: true) do |form| %>
  <% if locate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(locate.errors.count, "error") %> prohibited this locate from being saved:</h2>

      <ul>
      <% locate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :latitude %>
    <%= form.text_field :latitude, id: :locate_latitude %>
  </div>

  <div class="field">
    <%= form.label :longitude %>
    <%= form.text_field :longitude, id: :locate_longitude %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :locate_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
